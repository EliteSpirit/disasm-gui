Первая версия. Качаешь `DisasmGUI.exe`, запускаешь, Python ставить не надо.

Что умеет:

- открывает PE (.exe, .dll), ELF и сырые дампы; для дампа спросит архитектуру и базовый адрес
- x86 16/32/64, ARM, Thumb, ARM64, MIPS32, RISC-V 64
- подписывает импорты и экспорты, у ELF ещё символы и заглушки .plt (`printf@plt`)
- по `call` и `jmp` можно кликать, есть назад/вперёд (Esc, Alt+стрелки)
- поиск по тексту инструкций (`mov rax`) и по байтам (`0f 05`)
- переход к адресу или имени символа по Ctrl+G

Большие файлы окно не вешают: загрузка и поиск идут в фоне, а листинг подгружается кусками, пока крутишь.

Чего нет: декомпилятора, графа функций и перекрёстных ссылок. Это просмотрщик, а не замена IDA.

Windows SmartScreen может ругнуться на неподписанный exe, это нормально для таких сборок. Контрольная сумма лежит рядом в `DisasmGUI.exe.sha256`.
